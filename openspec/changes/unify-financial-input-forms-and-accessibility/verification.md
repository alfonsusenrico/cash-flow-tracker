# Form verification — 2026-09-24

## Debt-payment layout correction — 2026-09-25

- An owner screenshot exposed a nested responsive-grid defect: Quick Capture put the goal selector and debt editor into half-width columns, while debt rows attempted a three-column layout inside the narrow editor. The goal and debt controls now stack at full modal width; each debt row places the selector beside its remove action and its amount on a separate full-width line. Amount shortcuts wrap instead of clipping in a horizontal scroller. Allocation validation and API payloads did not change.
- The local frontend was rebuilt and recreated from image `2b0c4fd0eb3e`. Authenticated headless Chrome with a disposable synthetic account inspected two-debt Quick Capture at 1280px, 390px, and 320px. At all three widths, debt selectors and amount fields remained within the fieldset, amount fields used its available width, and the document had no horizontal overflow. The payment fields retained distinct accessible names; sampled dialog axe scans reported no violations. The synthetic account and fixtures were deleted after the run.
- Current frontend verification: 85/85 Vitest tests, lint, type-check, host and Docker production builds, strict OpenSpec validation, and `git diff --check` passed. The local login page returned HTTP 200. Actual browser zoom, physical-device keyboard/touch, and VoiceOver/TalkBack remain unverified; this does not close manual tasks 5.3 or 6.2.

The earlier checks below apply to the uncommitted worktree on `fix/financial-integrity-and-forms` based on `06c922c`; the debt-payment correction above applies to `fix/account-hierarchy-and-actions` at the same base commit. Browser checks used the local Docker app at `http://localhost:8090`, a fresh headless Chrome profile, and a disposable synthetic account. Each run deleted that account and its fixtures by exact user ID after the check. No production environment or existing user account was accessed.

## Observed browser behavior

- Authenticated Home, Accounts, Goals, Insights, and Ledger pages were inspected at 1280×800 and 390×844 CSS pixels. Ledger and Goals were additionally inspected at 320px; Ledger was inspected at 640px as a 200%-zoom-equivalent reflow width. The document had no horizontal overflow at those widths.
- Opened transaction capture/edit, movement, trade, account, category, recurring, payroll, goal, debt, goal-progress, registration, and settings flows were checked for available controls and associated input names. Shared dialogs initially placed focus inside; Escape closed the checked dialogs and restored focus to the triggering action in the verified Quick Capture path.
- The mobile debt-allocation form exposed two independently named debt selectors and amount fields in Chrome's accessibility tree. The live allocation summary updated, and the saved two-debt expense reloaded as one ledger transaction with two allocation rows in its edit form.
- At a simulated 390×420 CSS-pixel visual viewport, the ledger dialog scrolled, body scrolling remained locked, and the submit control could be brought fully into view. With `prefers-reduced-motion: reduce`, the dialog's computed animation name was `none`.
- Browser axe scans of the checked dark-theme dialogs found no remaining reported WCAG 2 A/AA, 2.1 A/AA, or 2.2 AA violations after the shared muted-text and goal-progress button contrast corrections. Sampled light-theme account, movement, buy, and sell dialogs also reported no violations.
- Owner screenshots exposed two layout defects missed by the synthetic-account pass: account cards stretched to a row peer while capping pocket lists inside a 250px nested scrollbar, and the recurring-rule list squeezed tabs, filters, amount, and actions into rows too narrow for their content. The account page now uses natural-height columns with page scrolling; the recurring dialog separates its tab/filter rows and places each rule's amount/actions on a full-width detail row. A fresh local Chrome check with six synthetic pockets found no card-internal scroll at 1280px (two 466px columns) or 2048px (three 560px columns), no document-level horizontal overflow, and no recurring-dialog horizontal overflow at a 448px dialog width. The disposable fixture was deleted afterward.

## Concise-copy revision — 2026-09-25

- The shared `FormField` and `FormSection` now present optional descriptions through a labelled information control. The control opens on hover or activation, supports Escape and outside dismissal, retains an accessible field description, and leaves validation errors inline. Redundant captions were removed across transaction, movement, category, recurring, goal, debt-allocation, account/pocket, and ledger-edit forms; essential conditional financial effects and errors remain visible.
- A local Chrome mobile check initially found the information panel extending beyond the 390px viewport near a bottom-sheet footer. The placement was corrected to choose above/below and clamp horizontal offset. A repeat browser check found the panel visible and entirely within the viewport. The account/pocket dialog was the browser fixture; the other affected forms are covered by source review and frontend component tests, not fresh individual browser checks.
- The final frontend suite passed 83/83 tests; lint, type-check, host and Docker production builds, strict validation of this OpenSpec change, and `git diff --check` passed. The local frontend container uses image `sha256:17de7c20dac1ec27efa2e920ac479ecf1ea5f44ff1fd462794b2d8a5c88ec8fa`; the API was not restarted.

## Limits and remaining manual checks

- The 640px check is a responsive-width equivalent, not proof of an actual browser at 200% zoom. The 420px visual-viewport check is not a physical on-screen keyboard test.
- Chrome's accessibility tree and keyboard event checks are not a VoiceOver or TalkBack session. Real screen-reader announcements, sequential Tab/Shift+Tab order for every form, physical touch targets, and device keyboard occlusion remain to be checked manually before claiming tasks 5.3 and 6.2 complete.
- These checks are spot evidence, not a WCAG conformance assessment.
