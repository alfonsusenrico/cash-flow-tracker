## 1. Core Formatting Utilities

- [x] 1.1 Implement `formatNumberWithDots` and `parseNumberFromDots` in `frontend/src/lib/utils.ts`

## 2. Goals & Obligations Forms

- [x] 2.1 Update Goals create/edit inputs (`goalTarget`, `goalCurrent`) and deposit modal (`actionAmount`) in `frontend/src/app/goals/page.tsx`
- [x] 2.2 Update Obligations create/edit inputs (`obTotal`, `obRemaining`, `obMinPayment`) and payment modal (`actionAmount`) in `frontend/src/app/goals/page.tsx`

## 3. Accounts, Ledger, Insights & Quick Capture

- [x] 3.1 Update Accounts modal inputs (`accInitBal`, `actualBalStr`, and `transferAmount`) in `frontend/src/app/accounts/page.tsx`
- [x] 3.2 Update Dashboard transfer modal input (`transferAmount`) in `frontend/src/app/page.tsx`
- [x] 3.3 Update Ledger edit transaction input (`editAmount`) in `frontend/src/app/ledger/page.tsx`
- [x] 3.4 Update Insights monthly budget input (`budgetInput`) in `frontend/src/app/insights/page.tsx`
- [x] 3.5 Update `QuickCaptureModal.tsx` to sanitize dots for math expressions and standard numbers

## 4. Verification & Container Rebuild

- [x] 4.1 Run frontend type-check, lint, and production build
- [x] 4.2 Rebuild and restart Docker containers and verify live inputs
